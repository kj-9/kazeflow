## 1. Run-result CLI presentation

- [x] 1.1 Update the single-owner `src/kazeflow/cli.py` run parser and text renderer with safe task and verbose attempt summaries.
- [x] 1.2 Add focused `tests/test_cli.py` coverage for ordered task summaries, failures/skips, verbose privacy, and verbose/JSON rejection.

## 2. User guidance

- [x] 2.1 Update `README.md` and `docs/cli.md` with the CLI pitch, quick-start, and partition selection examples.
- [x] 2.2 Update `docs/ROADMAP.md` with M12 scope and completion status.

## 3. Verification and integration

- [x] 3.1 Run focused CLI tests, full tests, `make ci-check`, and strict OpenSpec validation.
- [x] 3.2 Sync capability deltas to living specs, verify the completed change, archive it, and commit the integrated implementation.
